import styled from 'styled-components';
import { FormBox, LinkText, SubTitle } from '../../components/AuthForm/AuthForm.styles';
import { Button } from '../../styles/Button';


export const SignupSubTitle = styled(SubTitle)``;

export const SignupLinkText = styled(LinkText)``;

export const SignupFormBox = styled(FormBox)``;

export const SignupFormButton = styled(Button)``;
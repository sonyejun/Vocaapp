import styled from 'styled-components';
import { FormBox, LinkText, SubTitle } from '../../components/AuthForm/AuthForm.styles';
import { Button } from '../../styles/Button';


export const LoginSubTitle = styled(SubTitle)``;

export const LoginLinkText = styled(LinkText)``;

export const LoginFormBox = styled(FormBox)``;

export const LoginFormButton = styled(Button)``;